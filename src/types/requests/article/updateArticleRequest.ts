export default interface UpdateArticleRequest {
    id: String,
    name: String,
    description: String,
    price: Number,
    profilePicture: String,
    category: String,
    profileId: String,
}
