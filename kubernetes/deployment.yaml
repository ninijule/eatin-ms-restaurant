apiVersion: apps/v1
kind: Deployment
metadata:
  name: eatin-ms-profile-deployment
  labels:
    app: eatin-ms-profile
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eatin-ms-profile
  template:
    metadata:
      labels:
        app: eatin-ms-profile
    spec:
      containers:
        - name: eatin-ms-profile
          image: eatin-ms-profile
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
          env:
            - name: MONGODB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: mongo-root-username
            - name: MONGODB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: mongo-root-password
---
apiVersion: v1
kind: Service
metadata:
  name: eatin-ms-profile-service
spec:
  selector:
    app: eatin-ms-profile
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
